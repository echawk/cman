Implement a unit testing framework; need to test helper methods.
	- look into greatest as it is c89 compilant

Break up cman.c into separate files.

Implement some type of ghost enemies; write a different AI for
each of them.

Add in color support; ncurses has a way to do this?

Use pthreads and sockets to make the game multithreaded; this will
require some substantial effort.

Add in a more algorithmic way to spawn walls; maybe have there be a preference for walls to spawn next to each other?
But not so much that they block off the entire map
	- got some initial work on this done.

Document the code! Also go through and refactor after this; there will definitely be some pain points"
